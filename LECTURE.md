# 따라하기 실습 — 성향 테스트를 검색 도구로 바꾸기

**목표:** 단순 MBTI 테스트였던 웹앱을, 실제 법원경매 물건을 거르는 **검색 도구**로 바꿉니다.

| | 링크 |
|---|---|
| 🟡 **시작점** (개편 전) | https://earthskyisbig.github.io/auction_mbti/v1/ |
| 🟢 **완성본** (개편 후) | https://earthskyisbig.github.io/auction_mbti/ |
| 🎨 디자인 시스템 | https://earthskyisbig.github.io/auction_mbti/design-system/ |

두 링크를 나란히 열어두고 시작하세요. **무엇이 달라지는지 눈으로 먼저 보는 게 중요합니다.**

---

## 1. 시작점 받기

```bash
git clone --single-branch -b lecture-base https://github.com/earthskyisbig/auction_mbti.git auction_mbti_실습
cd auction_mbti_실습
```

`lecture-base` 브랜치가 **개편 전 상태로 고정**돼 있습니다.

> 💡 **`--single-branch`를 꼭 붙이세요.** 빼면 완성본(`main` 브랜치)까지 같이 받아져서,
> `git checkout main` 한 번이면 정답이 다 보입니다. 먼저 직접 해보는 게 실습의 전부입니다.

---

## 1-1. 내 깃허브에 저장하기

**받은 코드를 내 저장소에 올려도 됩니다.** 강사 저장소에는 아무 영향이 없습니다 —
수강생 계정에는 **읽기 권한만** 있어서 강사 저장소로는 push 자체가 거절됩니다(403).

### ① 내 GitHub에 빈 저장소 만들기

github.com → **New repository** → 이름 입력 →
**README·.gitignore·license 추가는 모두 체크 해제** (빈 상태여야 충돌이 없습니다) → Create.

### ② remote를 내 것으로 바꾸고 올리기

```bash
# 지금 origin은 강사 저장소를 가리킵니다. 내 저장소로 교체하세요.
git remote set-url origin https://github.com/내아이디/내-저장소.git

# 브랜치 이름을 main으로 (내 저장소에서는 이게 자연스럽습니다)
git branch -M main

git push -u origin main
```

이후로는 평소처럼 `git add` → `git commit` → `git push` 하면 **내 저장소에만** 쌓입니다.

### 확인

```bash
git remote -v     # 강사 주소가 아니라 내 주소가 나와야 정상
```

<details>
<summary>강사 저장소의 업데이트도 계속 받고 싶다면 (선택)</summary>

```bash
git remote add upstream https://github.com/earthskyisbig/auction_mbti.git
git fetch upstream
git log --oneline upstream/main    # 완성본 커밋 보기 (스포일러 주의)
```

`upstream`은 **읽기 전용**으로만 동작합니다. 여기로 push하면 거절됩니다.

</details>

> ⚠️ **Fork 버튼을 쓰지 않는 이유**
> Fork도 되지만 내 저장소가 강사 저장소에 **연결된 상태**로 남아서, Pull Request를 만들면
> 기본 대상이 강사 저장소가 됩니다. 실습 결과물을 내 것으로 독립시키려면 위 방식이 깔끔합니다.

받은 뒤 `index.html`을 더블클릭하면 브라우저에서 바로 열립니다. **설치할 게 없습니다** — 단일 HTML 파일이라서요.

```
index.html                    ← 실제 앱 (869줄, 전부 여기에 들어있음)
부동산투자성향테스트.html        ← index.html의 원본 (publish.sh가 복사해 만듦)
_workspace/                   ← 유형 체계·문항·규제 근거 문서
.claude/                      ← 에이전트 하네스 (agents 6 + skills 6)
CLAUDE.md                     ← 프로젝트 규칙
```

---

## 2. 먼저 문제를 찾습니다

코드를 고치기 전에 **무엇이 잘못됐는지** 스스로 찾아보세요. 이게 실습의 핵심입니다.

시작점 앱을 열어 경매 트랙을 끝까지 진행한 뒤, 아래를 확인해 보세요.

- [ ] 설문 **2번, 14번, 15번, 16번**을 다시 읽어보세요. 무엇이 이상한가요?
- [ ] 결과 화면 맨 아래 「추천 경매 검색조건」에서 **시·군·구를 바꿔** 보세요. 그 값은 어디에 저장될까요?
- [ ] 자금을 5천만원에서 3억으로 바꾸면 **유형 코드가 바뀝니다.** 자금이 성격인가요?

### 정답

<details>
<summary>펼쳐보기 (먼저 스스로 찾아본 뒤에)</summary>

**① 같은 질문을 네 번 묻습니다.** 2·14·15·16번이 전부 "보유 vs 매각" 하나를 되묻습니다. 세 문항은 배점까지 `{r:2}/{f:2}`로 동일합니다.

**② 검색값이 모델에 자리가 없습니다.** `computeSearchCriteria(r, sido, sigungu)` — 시·군·구를 담을 곳이 없어 **함수 인자로 새어 들어옵니다.** 실제 값은 결과화면 DOM에만 존재해, 새로고침하면 사라집니다.

**③ 일회성 조건이 영구적 성격을 바꿉니다.** `scoreTrack()`이 `profile_inputs`(자금·지역)의 점수를 성향 문항과 **같은 tally에 합산**합니다. "이번에 자금 5천만원"이 "이 사람은 소액분산형 체질"이라는 판정을 바꿉니다.

세 문제의 뿌리는 하나입니다 — **투자 체질과 이번 검색조건이 분리돼 있지 않습니다.**

</details>

---

## 3. 실습 단계

완성본의 커밋 4개가 그대로 실습 단계입니다. 각 단계 끝에서 동작을 확인하고 넘어가세요.

### STEP 1 — 2레이어로 분리하기

성격이 다른 두 가지를 나눕니다.

- **Layer 1 `InvestorProfile`** — 잘 안 바뀌는 투자 체질. 권리 / 명도 / 수익 / 기간 4축.
  **검색값을 담지 않고** 제외규칙·가중치만 만듭니다.
- **Layer 2 `FilterMode`** — 매번 바뀌는 검색값. 예산·지역·자산유형·유찰수·최저가율…
- **병합** — `buildSearchQuery(profile, filterMode)`가 둘을 합치고, 모든 결정의 근거를 `rationale[]`에 남깁니다.

> 💡 **여기서 배울 것:** 기존 16유형 콘텐츠(15,339자)를 버리지 않고 새 구조를 얹는 법.
> 유형 이름이 **검색값으로 지어져 있어**(“**동네** 안전 낙찰러”, “**소액** 특수 단타러”) 축을 그냥 빼면 16유형이 4유형으로 붕괴합니다.

**확인:** 진단 없이도 검색조건 화면에 들어가지나요? 프리셋이 저장·재사용되나요?

### STEP 2 — 중복 문항 고치기

2·14·15·16번이 서로 **다른 판단**을 묻게 다시 씁니다.

| 문항 | 바뀐 뒤 묻는 것 |
|---|---|
| 14 | 입찰 상한선을 무엇으로 역산하나 (가치 산정 기준) |
| 15 | 6개월 조용할 때의 심정 (시간 압박) |
| 16 | 다음 물건 자금의 출처 (자본 재조달) |

> ⚠️ **제약:** 공유 URL(`?r=`)이 **문항 ID + 선택지 인덱스**로 저장됩니다.
> 문구는 바꿔도 되지만 **ID·선택지 순서·배점을 바꾸면 기존 링크가 깨집니다.**

**확인:** 개편 전에 만든 공유 링크가 여전히 같은 유형을 내나요?

### STEP 3 — 결과를 읽을 수 있게 만들기

결과 화면이 “당신은 A-scnr입니다”를 열 가지 방식으로 반복하면서, 정작 **“그래서 뭘 하지”**에는 답하지 않습니다.

- **흐름 맵** — 진단 → 체질 → 검색조건 → 물건 찾기. 지금 어느 단계인지 표시
- **스펙트럼** — 축을 글자 하나로 찍지 말고 **어느 쪽으로 얼마나** 기울었는지 (`tally`가 이미 있는데 버려지고 있습니다)
- **안내 한 줄** — 섹션마다 “이건 무엇을 보는 것인가”
- **접기** — 내부 자료구조(`exclusions`/`weights` 원본)는 숨기고, 무슨 일을 하는지 문장으로

**확인:** 처음 보는 사람이 결과 화면만 보고 다음에 뭘 할지 알 수 있나요?

### STEP 4 — 디자인 시스템 입히기

Mono Signal(다크 + 라임 액센트)을 적용하고, 다른 프로젝트에서도 쓸 수 있게 분리합니다.

- 하드코딩된 색 37종을 토큰으로
- 다크/라이트 2테마 + 우상단 토글 (**라이트 모드에는 라임을 남기지 않습니다** — 대비가 무너져요)
- 재사용 가능한 부분만 뽑아 `design-system/`으로

**확인:** 색을 바꿀 때 개별 규칙이 아니라 `:root` 토큰 한 곳만 고치면 되나요?

---

## 4. 확인하는 법

완성본에는 셀프테스트 20개가 들어있습니다.

```bash
# 브라우저 — 주소 뒤에 ?selftest=1
# 예: index.html?selftest=1

# 터미널 (Node 필요, 실패 시 exit 1)
node _workspace/tools/selftest_node.js 부동산투자성향테스트.html
```

**직접 짜볼 만한 테스트**

| | 무엇을 지키는가 |
|---|---|
| 레이어 격리 | `InvestorProfile`에 예산·지역 키가 **없다** |
| 불변식 | 검색조건만 바꿔도 유형 코드가 **안 변한다** |
| 회귀 | 개편 전 공유 URL이 **같은 결과**를 낸다 |
| 문항 중복 | 같은 축 문항끼리 어휘 중복도가 임계값 이하 |

> 💡 4번은 실제로 **실패한 적이 있는 검사**입니다. 동의어를 쓰면 어휘가 안 겹쳐서 통과해 버립니다.
> 자동 검사가 사람 눈을 대체하지 못한다는 사례로 보세요.

---

## 5. 막혔을 때

| 증상 | 확인할 것 |
|---|---|
| 고쳤는데 반영이 안 됨 | **`index.html`을 직접 고치지 마세요.** 원본은 `부동산투자성향테스트.html`이고 `index.html`은 `publish.sh`가 만드는 복사본입니다 |
| 공유 링크가 다른 유형을 냄 | 문항 ID·선택지 순서·배점을 건드렸는지 확인 |
| 라이트 모드가 안 읽힘 | 액센트가 라임으로 남아있는지 확인. 밝은 배경 위 라임은 대비가 무너집니다 |
| 한 화면에 라임이 너무 많음 | 액센트는 **하나**입니다. 한 화면에 서너 군데면 충분합니다 |

---

## 6. 더 해볼 것

완성본도 끝이 아닙니다. 다음이 남아 있습니다.

- 저장된 프리셋으로 **매일 자동 검색** 돌리기
- 검색 결과를 실제 경매 DB(`auction_item`)와 연결하기
- 가중치로 물건 **순위 매기기**
- 조건에 맞는 새 물건이 뜨면 **알림 보내기**

`buildSearchQuery()`의 출력(`query` / `postFilters` / `weights`)이 그 입력 규격입니다.
설계 문서: [`_workspace/05_data_model.md`](_workspace/05_data_model.md)

---

## ⚠️ 면책

이 앱과 실습 자료는 **투자 참고용**이며 세무·법률·투자 자문이 아닙니다.
부동산 규제·세율은 수시로 바뀝니다. 실제 매매·입찰·대출·세금 적용 전에는 반드시 **금융기관·세무사·법무사**와 해당 시점 규정을 직접 확인하세요.
